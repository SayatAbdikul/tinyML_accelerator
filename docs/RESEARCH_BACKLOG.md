# Ushqyn implementation backlog

Companion to the [32-week research roadmap](/Users/sayat/Documents/GitHub/tinyML_accelerator/docs/RESEARCH_ROADMAP.md). Checkboxes reflect the [phase 0–1 evidence](research/PHASE_0_1_STATUS.md); partial tasks remain unchecked. One full-time researcher owns the critical path. Paths below describe existing areas to change or proposed files to create beneath `/Users/sayat/Documents/GitHub/tinyML_accelerator`.

Complete tasks in dependency order. Split an implementation task into smaller reviewable commits where useful, but keep its definition of done. Evidence belongs in a versioned manifest and durable artifact location, not only terminal history. Never remove the known semantic regressions to obtain a green build.

## P0 — weeks 1–2: freeze the problem and evidence

- [ ] **R01 — Freeze the claim and closest prior work.** Read full FINN-R, SAMO, DNNExplorer, Fused-Layer CNN, MCUNetV2/msf-CNN and the most relevant newer work; inspect available implementations. Create `docs/research/novelty_matrix.md` with supported/unsupported/unknown features and primary citations. Select B3 and explain why it is the closest applicable baseline. **Done:** a one-paragraph candidate novelty statement, one falsifiable hypothesis, explicit SOTA comparison eligibility, and a documented overlap/pivot decision. **Depends:** none.
- [ ] **R02 — Freeze KWS, VWW and AD artifacts.** Add `benchmarks/manifests/` and conversion/inventory tooling. Pin model/source/evaluator/dataset/calibration hashes; collect every operator, attribute, tensor shape, MAC count and storage requirement. **Done:** source-framework versus converted-float parity within declared numerical tolerances, reproducible model inventories, and no unknown required operator. KWS/VWW each have a primary manifest. **Depends:** none.
- [x] **R03 — Preserve baseline provenance and test evidence.** Archive source revision, known dirty changes, compiler tests, current failing semantic probes, historical claims, and tool availability. Save whatever historical synthesis evidence is actually available; missing evidence stays missing. Add environment locks/manifests. **Done:** another person can distinguish measured, reproduced, reported, and derived claims; the five confirmed semantic probes are reproducible. **Depends:** none.
- [ ] **R04 — Establish lab and measurement access.** Verify Tang Nano board revision, programming tool, UART path and Gowin flow; arrange power instrumentation and a second-target validation route if available. Define clock, supply and timing-marker setup. **Done:** reproducible minimal board build, UART loopback/readback, instrument acquisition/borrow plan, and a recorded actual capability/constraint list. **Depends:** none.

**Current G0: open** — R01/R02/R04 remain partial; see the phase status.

**G0:** R01–R04 reviewed. Use full early effort on unresolved model import or missing lab access rather than inventing benchmark estimates.

## P1 — weeks 3–5: make the numerical model correct

- [x] **Q01 — Specify arithmetic and add independent regression cases.** Add `docs/specs/quantization.md` and a test-only independent integer operator reference. Define INT8 ranges/zero points, per-channel scales, INT32 bias units, rounding (including negative ties), shifts, intermediate widths, saturation and accumulator bounds. Preserve bias-magnitude/class-flip, BN, large ReLU, terminal Add and bias-free Conv cases. **Done:** arithmetic can be evaluated without calling compiler lowering/golden helpers; current defects fail targeted assertions before repair. **Depends:** R02, R03.
- [x] **Q02 — Canonicalize and validate graph semantics.** Introduce a preallocation pass in `compiler/compile.py` or a dedicated module. Fold inference BN, preserve all outputs, create zero biases, handle layouts explicitly, and reject unsupported operations/attributes. Remove BN passthrough assumptions in `compiler/buffer_allocator.py`. Validate Conv groups/dilation/independent axes, pooling rounding/padding, Gemm transpose/alpha/beta and broadcast semantics. **Done:** all known graph-semantic regressions pass or fail explicitly at compile time where the op remains unsupported; no executable is emitted for a rejected graph. **Depends:** Q01.
- [x] **Q03 — Preserve quantization metadata end to end.** Add typed tensor/layer IR; quantize weights per channel, biases to product-scale INT32, and activations using saved calibration. Update `compiler/dram.py`, `helper_functions.py`, lowering and reference execution. Make metadata survive folding, layout conversion and allocation. **Done:** independent integer operators and instruction-level simulation agree exactly on supported graphs; bias scaling example selects the correct class; missing/invalid scales are rejected. **Depends:** Q01, Q02.
- [x] **Q04 — Make image layout safe and versioned.** Replace fixed overlapping DRAM-region assumptions with explicit aligned segments for instructions, tensors, biases, multipliers/shifts and descriptors. Add bounds, collision, length/field-range and target checks in assembler/image generation. **Done:** every byte has an owned nonoverlapping region; oversized models fail before a partial image is used; target/numerical version mismatch is detectable. **Depends:** Q03.
- [x] **Q05 — Separate validation layers and complete software quality evaluation.** Restore strict exact-match assertions and separate classification mistakes from implementation mismatches. Prepare multi-channel/multi-tile integration cases and primary-model boundary cases; record RTL migration failures until H03 implements corrected arithmetic, with no blanket forced-pass flags. Evaluate complete pinned KWS/VWW sets through an architecture-independent quantized-graph evaluator, keeping calibration/test sets disjoint. **Done at G1:** zero integer-reference versus instruction-simulator mismatches for the supported instruction subset; unsupported hardware graphs reject explicitly; source conversion parity and full quantized-graph primary quality targets hold. Passing corrected RTL integration is required later at H05/C03/B01, not before its implementation exists. **Depends:** Q02–Q04.

**Current G1 software result:** Q01–Q05 pass for the declared KWS/VWW research splits; G0 and official MLPerf eligibility remain open.

**G1:** Q01–Q05 pass. Stop here if the imported network's numerical semantics are still uncertain.

## P2 — weeks 6–8: one tested board implementation

- [ ] **H01 — Generate explicit target/config and source manifests.** Refactor `compiler/accelerator_config.py`, `generate_config.py`, decoder generation and build scripts. Consolidate active compute/control RTL under `rtl/`; retain narrowly scoped board/IP wrappers in `src/` or an explicitly named platform directory. **Done:** compiler, board simulation and synthesis consume one selected target/numerical manifest; no hand-maintained active duplicate decoder or arithmetic/control module. Width truncation and illegal geometry fail elaboration/compilation. **Depends:** G1.
- [ ] **H02 — Define descriptors and memory interfaces.** Specify layer/tile geometry, bases/strides, quantization pointer, datatype and operation flags; support independent kernel/stride/padding axes and logical dimensions beyond 63. Specify memory request/response, backpressure, completion and error semantics. **Done:** reference encoder/decoder round trips; current 24-bit address fields are propagated where needed; out-of-range descriptors are rejected; maximum count/address widths are derived and checked. **Depends:** Q04, H01.
- [ ] **H03 — Implement the static requantizer in shared RTL.** Update quantization and execution interfaces for INT32 bias and per-channel multiplier/shift/output zero point. Preserve the eight-bit MAC input contract using compiler-computed corrected bias `qb - zx * sum(qw)`; check correction/intermediate ranges before packing and apply it once per output. Freeze the integer algorithm chosen in Q01; use sufficient product width and defined pipeline valid timing. **Done:** directed boundary/tie/overflow-rejection and random vector tests match the independent reference exactly, including extreme input zero points, channel transitions and stalled interfaces. **Depends:** Q01, Q03, H01.
- [ ] **H04 — Add reliable addressable host commands.** Implement framed READ/WRITE, RUN(start-PC), STATUS, RESET/ABORT and capability/version queries in board control and host tooling. Define checksum/framing/length recovery. RUN resets program/execution state and counters while preserving weights. Define writes and commands while busy. **Done:** automated load-once/change-input/run/read operation; malformed/truncated commands, wrong image version and aborted execution recover predictably; no button dependence. **Depends:** H01, H02.
- [ ] **H05 — Add profiling and test the real board top.** Integrate counters for elapsed/compute/wait cycles, useful MACs, transfer bytes, errors and per-layer markers. Use behavioral models of the actual synchronous memory interfaces in the board-hierarchy test. Correct the MLP's board storage profile to fit. **Done:** corrected-MLP strict RTL checks prepared in Q05 pass; 1,000 automated board inferences match the oracle; counters reconcile under documented overlap rules; routed 27-MHz timing/resource report and real board output are archived. **Depends:** H03, H04.
- [ ] **R05 — Validate the early novelty counterexample.** Build a small problem showing why the chosen B3/separate allocator misses physical bank/port/quantized-live-state constraints, if it does. Validate against synchronous memory microbenchmarks; draft the two-page argument. **Done:** an explained and reproducible counterexample or an explicit pivot decision; no unsupported claim that existing work lacks a feature. **Depends:** R01, H02; may use H05 counters when ready.

**G2:** H01–H05 pass, R05 decision recorded. Freeze numerical/descriptor interfaces before the main memory rewrite.

## P3 — weeks 9–12: a BSRAM-efficient SmallCNN

- [ ] **M01 — Specify physical bank organization and a feasibility checker.** Define width/depth modes, port availability, reserved storage and alignment. Build `compiler/memory_planner.py` and a separate verifier emitting logical bytes, physical blocks, lifetimes and access demand. **Done:** legal allocations pass independent overlap/bounds/bank checks; deliberately infeasible allocations fail; predicted block count is checked against synthesized representative memories. **Depends:** H02.
- [ ] **M02 — Replace whole-buffer arrays with the shared scratchpad.** Refactor buffer controller/file interfaces to descriptor-based synchronous bank accesses. Allocate vectors, weights, partial sums and metadata from actual live demand. Add explicit lifetime ownership and completion. **Done:** read/write hazards, full capacity, reuse and tail transfers pass; no old full tensor-bank copy remains active unintentionally; initial resource budget leaves system-integration headroom. **Depends:** M01, H01.
- [ ] **C01 — Implement the shared tiled FC/ordinary-Conv datapath.** One eight-lane engine; bounded output-stationary partial sums; input-channel accumulation across tiles; weight reuse; activation broadcast where appropriate. Multiply raw INT8 `qx * qw` and add the full-reduction corrected bias from H03 once. Emit padded activations as `qx = zx`; do not skip those products without adjusting the correction. Replace hot dynamic division/modulo address operations with counters/precomputed strides. **Done:** FC, rectangular/1×1/3×3 Conv, channel/spatial tails, signed extremes and extreme zero points, unequal strides/padding and multi-tile partial sums match the oracle; correction/partial-sum bounds checked; shared engine appears once in synthesis. **Depends:** M02, H03.
- [ ] **C02 — Implement window generation and legal streaming activation/pooling.** Use synchronous line/window buffers and defined valid/backpressure. Apply zero-point padding correctly; handle ReLU and clamped activation semantics. Retain intermediate quantization where required. **Done:** boundary/odd-size/asymmetric-padding and backpressure tests pass, including primary-model-derived shapes; no asynchronous multiport register-array substitution hides BSRAM costs. **Depends:** C01.
- [ ] **C03 — Release a complete on-chip SmallCNN.** Integrate image compiler, descriptors, engine, memory and commands. Run full MNIST quality/board comparison and 1,000 repeated jobs. Create deterministic build/program/run scripts and save raw reports. **Done:** Q05's corrected SmallCNN strict RTL checks pass; zero board integer-output mismatches; all device resources fit; declared clock meets post-route timing; layer cycles/storage/traffic and host-versus-core latency reported. **Depends:** C01, C02, H05.

**G3:** C03 passes. This is the first major accelerator milestone. SDRAM is deliberately not a prerequisite for this small model.

## P4 — weeks 13–17: complete audio/vision kernels and external memory

- [ ] **C04 — Complete depthwise, pointwise and average-pool support.** Reuse the MAC engine with a depthwise lane/address mode, support the KWS 10×4 first Conv and VWW stride-2 boundaries, and implement required average/global-average pooling and activation clamps. **Done:** every compute/activation/pooling node in both frozen primary inventories has exact independent-reference/RTL tests, including average-pool rounding and zero points. **Depends:** G3, R02.
- [ ] **D01 — Bring up the SDR SDRAM controller independently.** Integrate a documented GW2AR-compatible controller/IP with initialization, refresh, full address reach and declared clocking. Test walking/random/address-dependent patterns and row/bank/burst boundaries. **Done:** full 8-MiB range verified without aliasing; at least 1 GiB aggregate randomized read/write traffic passes with refresh active; supported timing and reset/reinitialization recorded. **Depends:** G3, H02.
- [ ] **D02 — Add DMA and double-buffer overlap.** Implement burst DMA with partial tails, backpressure, arbitration, completion ordering and ping-pong tiles. Integrate compiler transfers and live-region protection. **Done:** exact data under randomized stalls/refresh/reset cases; no overwrite of live input/partial sums; useful sustained bandwidth and compute/transfer overlap measured, with protocol overhead included. **Depends:** D01, M02.
- [ ] **D03 — Validate tiled inference with off-chip placement.** Run known SmallCNN/MLP tensors in SDRAM, then large synthetic FC and real AD layer shapes. Audit all downstream address/count widths. **Done:** outputs agree with BSRAM/reference cases, high addresses work, weights larger than one tile execute correctly, and latency accounts for all transfers. **Depends:** D02, C04.
- [ ] **P01 — Build the measured kernel/cost database.** Profile ordinary/DW/PW/FC/pooling/quantization plus DMA across representative shapes and bandwidth settings. Include short/tail kernels. Split configurations into fitting and validation sets before fitting. **Done:** machine-readable cycles, stalls, traffic, physical blocks and energy where available; a reproducible initial latency/energy model with held-out errors. **Depends:** H05, C04, D02.

**G4:** C04, D01–D03 and P01 pass. The pinned model inventories, not assumptions about “typical CNNs,” define kernel completeness.

## P5 — weeks 18–20: credible complete-model baselines

- [ ] **B01 — Deploy KWS and VWW on the same release bitstream.** Add complete pipelines for both pinned models and declared host preprocessing/postprocessing. Integrate with the pinned evaluator/runner. **Done:** complete accuracy sets meet targets; integer outputs match; 10,000 automated balanced audio/vision jobs pass; model switching needs no RTL changes or reflashing. **Depends:** G4, Q05.
- [ ] **B02 — Deploy AD as the third workload.** Preserve linear reconstruction output and pinned anomaly-scoring semantics. Stream its large weights through DMA. **Done:** exact output validation and complete-set ≥0.85 ROC-AUC; scoring time and hardware/host boundary documented. **Depends:** B01, D03.
- [ ] **B03 — Freeze and tune B1/B2/B3 fairly.** Implement a conventional tiled baseline, physical-liveness-only version, and the selected closest published policy/adaptation. Match arithmetic, engine, clock, bank/SDRAM budget and comparison boundary; document each tuning budget and unavailable feature. **Done:** strong feasible baseline results for both primary models, with all failures/limitations retained and no intentionally weakened baseline. **Depends:** R01, B01, P01.

**G5:** B01 and B03 required; B02 completes the planned suite. If late, finish both primary workloads before allocating further effort to AD variants or optional features.

## P6 — weeks 21–25: prove the proposed mechanism matters

- [ ] **S01 — Build legal schedule candidates and certificates.** Add `compiler/scheduler/` with fixed-quantized-IR candidates, halos, tensor live ranges, partial sums, quantization barriers, banks/ports and transfer edges. Start with layer-wise and a bounded fused-segment catalog. **Done:** independent checker accepts every emitted schedule; hand-derived and enumerated cases validate legality; no arithmetic transform silently changes rounding/scales. **Depends:** R05, M01, B03.
- [ ] **S02 — Implement exact small-instance search.** Enumerate or use DP with sufficient frontier/live-bank state; preserve feasibility-relevant distinctions in dominance pruning. **Done:** exact search agrees with brute force on a published small-instance suite; optimality claims explicitly limited to the candidate catalog. **Depends:** S01.
- [ ] **S03 — Implement bounded large-instance optimization.** Add beam/Pareto search, explicit time budget, deterministic seeds, measured costs, valid lower bounds and a feasible fallback. **Done:** no false feasible placements; deterministic outputs for fixed settings; search-quality/runtime evaluated against the best explored candidate set, with a target ≤10% objective gap and ≤60 s/model on the declared host. **Depends:** S02, P01.
- [ ] **S04 — Run complete B4 and causal ablations.** Compare against B1/B2/B3; disable bank-aware costs, joint placement, fusion and overlap/recomputation independently. Validate predictions on held-out layers/configurations. **Done:** at least one measured improvement explained by the proposed mechanism on meaningful workloads; report cost errors, negative cases and all resource/quality constraints. Apply G6 kill criteria before expanding scope. **Depends:** S03, B03.
- [ ] **S05 — Optional dynamic-quantization storage experiment.** Only after S04 establishes the main contribution, define correct dynamic bias scales and preserve original global max-absolute reduction while pooling INT32 outputs before quantization. Prove the rewrite's scope and test negative/border/tie cases. **Done:** exact outputs versus the correct dynamic reference, actual BSRAM/traffic/energy changes measured, and quality-calibration comparison against static mode separated. Omit if it distracts from both primary workloads or yields no useful tradeoff. **Depends:** S04, Q01.

**G6:** S01–S04 pass and the research claim survives B3. S05 is not required. New INT4/sparsity/16-lane work needs a measured bottleneck rationale and must not delay G7.

## P7 — weeks 26–29: collect defensible research evidence

- [ ] **E01 — Freeze experimental manifests and run the full matrix.** Run the roadmap's 32 primary and 16 secondary budget/baseline configurations, marking infeasible points. Include common-clock and independently timing-closed comparisons and actual physical builds for area/energy claims. **Done:** complete accuracy/latency/resource/traffic results trace to artifacts; no missing point silently disappears from summaries. **Depends:** G6, B02.
- [ ] **E02 — Measure board energy and uncertainty.** GPIO-triggered voltage/current integration; at least five independent sessions in randomized configuration order, ≥10-second windows or instrument-appropriate repeated runs. Include all relevant supply paths, gross energy and separately labeled idle subtraction. **Done:** raw traces, instrument setup/calibration, statistics, sample counts and 95% intervals archived; no energy claim depends only on a synthesis estimate. **Depends:** R04, E01.
- [ ] **E03 — Validate robustness and generalization.** Test unseen shapes, at least one extra model variant per primary family where claiming family-level results, bandwidth throttling, and a second FPGA target. Record multiple P&R seeds if supported. **Done:** failures and limits published; measured versus simulated/post-route-only evidence separated; held-out cost-model error reported without refitting on the test split. **Depends:** S04, E01.
- [ ] **E04 — Obtain independent reproduction and decide the final claim.** Have another person reproduce a clean build, one audio run, one vision run and representative plots from the documented artifact. Recheck current prior work/implementations. **Done:** reproduction issues resolved; scope of SOTA claim exactly matches eligible comparisons and improved metrics; remaining limits are explicit. **Depends:** E01–E03.

**G7:** E01–E04 pass. If the result is competitive but does not establish SOTA, use the strongest supported claim. A paper cannot inherit a SOTA label from the roadmap.

## P8 — weeks 30–32: prepare the submission

- [ ] **W01 — Generate every paper figure from frozen data.** Add `paper/` and plotting/table scripts; include motivation, architecture, schedules, Pareto curves, memory/traffic, ablations, cost-model/search validation, quality/resources and limitations. **Done:** no manually entered benchmark result lacks a source record; a single documented command regenerates the main figures. **Depends:** G7.
- [ ] **W02 — Write and review the manuscript.** Lead with the established problem/mechanism/result, distinguish engineering from novelty, compare full closest work, and include negative cases. Obtain at least two technical reviews if possible. Choose the actual venue/cycle using its current rules. **Done:** every contribution sentence has evidence; no undocumented boundary/accuracy/platform normalization; critiques have tracked resolutions. **Depends:** W01.
- [ ] **W03 — Package the artifact and submission materials.** Include source/build/config manifests, models or licensed download instructions, test vectors, scripts, reports, raw data and figure regeneration; document proprietary tools/IP and target setup. Follow venue-specific anonymity/artifact instructions. **Done:** clean-room reproduction recipe, permanent archive plan, artifact checklist and a ready submission package. Submission/publication is a separate explicit action. **Depends:** W02, E04.

**G8:** W01–W03 complete. Acceptance remains the venue's decision; the deliverable is a credible, reproducible paper and accelerator.

## First ten working days

This starts P0 and the first Q01 preparation; it does not assume the later compiler or board fixes are already available.

| Day | Main work | Reviewable output |
|---:|---|---|
| 1 | Preserve revision/known failures; verify board/tool access | R03 manifest draft, recorded failures, R04 access checklist |
| 2 | Pin audio/vision/AD model and evaluator artifacts | R02 manifests with hashes |
| 3 | Generate model operator/shape/storage inventories | Three inventories; missing-operator list |
| 4 | Validate source-to-ONNX conversion and preprocessing | Float parity report for each model |
| 5 | Read closest fusion/mapping papers and inspect code | R01 novelty matrix first draft |
| 6 | Complete prior-work comparison and choose B3 | Written candidate claim, baseline choice and counterexample plan |
| 7 | Define integer arithmetic, bias/scales and rounding | Q01 executable-spec draft |
| 8 | Turn the five confirmed semantic failures into isolated regressions | Tests that expose current defects without shared-oracle assumptions |
| 9 | Define full target/descriptor/memory budgets from actual models | Architecture contract draft; instrumentation setup plan |
| 10 | Review G0; prioritize P1 and approve the evidence contract internally | Completed G0 decision record and next two weeks' implementation queue |

Review each Friday: passed tests/gates, one audio result, one vision result, measured bottleneck, most serious unresolved risk, and the next smallest decisive experiment. If a primary model is not yet running, its result is an explicit import/kernel/memory progress report rather than a fabricated performance estimate.
